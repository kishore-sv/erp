docker-compose up -d


docker exec -it postgres-primary psql -U postgres -d erp_db -c "SELECT client_addr, state, sync_state FROM pg_stat_replication;"


docker exec -it postgres-replica psql -U postgres -d erp_db -c "SELECT pg_is_in_recovery();"


./scripts/backup.sh


0 2 * * * /path/to/your/project/server/scripts/backup.sh >> /path/to/your/project/server/backups/backup.log 2>&1
